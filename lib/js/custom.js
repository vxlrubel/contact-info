;(function($){
    const doc = $(document);

    class ContactInfo{
        init(){
            
        }
    }
    
    doc.ready(()=>{

        const CONTACT_INFO = new ContactInfo();

        CONTACT_INFO.init();
        
    });
})(jQuery);